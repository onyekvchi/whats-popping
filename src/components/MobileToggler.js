import React from "react";

const MobileToggler = ({ isMenuOpened }) => {
  return (
    <button className="menu-toggle">
      <span />
      <span />
      <span />
    </button>
  );
};

export default MobileToggler;
